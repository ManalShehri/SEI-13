/*
 * You first task is to make it so that when a user clicks on any of the 100 squares
 * on the page, the console says "You clicked a square!".
 *
 * Your second task is to make it so that, when the user clicks on a square, the console
 * tells them the number button they clicked on (i.e. "You clicked button
 * number 74")
 * HINT: .setAttribute('id', ____ ) in a for loop should set the id property on all butotns
 */
